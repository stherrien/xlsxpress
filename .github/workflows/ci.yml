name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # Rust tests and linting
  rust-test:
    name: Rust Tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache target directory
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-target-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Run clippy
        run: |
          export PYO3_PYTHON=$(which python3)
          if [ "$RUNNER_OS" == "macOS" ]; then
            export DYLD_LIBRARY_PATH=$(python3 -c "import sysconfig; print(sysconfig.get_config_var('LIBDIR'))"):$DYLD_LIBRARY_PATH
          fi
          cargo clippy --all-targets --all-features -- -D warnings
        shell: bash

      - name: Install maturin for fixtures
        run: pip install maturin

      - name: Build for test fixtures
        run: |
          export PYO3_PYTHON=$(which python3)
          if [ "$RUNNER_OS" == "macOS" ]; then
            export DYLD_LIBRARY_PATH=$(python3 -c "import sysconfig; print(sysconfig.get_config_var('LIBDIR'))"):$DYLD_LIBRARY_PATH
          fi
          maturin build --release
        shell: bash

      - name: Install for test fixtures
        run: |
          python -m pip install --find-links=target/wheels xlsxpress
        shell: bash

      - name: Create test fixtures
        run: |
          mkdir -p tests/fixtures
          python -c "
          import xlsxpress
          w = xlsxpress.Writer()
          s = w.add_worksheet('Sheet1')
          w.write_string(s, 0, 0, 'Hello')
          w.write_number(s, 0, 1, 42.0)
          w.write_string(s, 1, 0, 'World')
          w.write_number(s, 1, 1, 3.14)
          w.write_string(s, 2, 0, 'Test')
          w.write_number(s, 2, 1, 100.0)
          w.save('tests/fixtures/test.xlsx')
          "
        shell: bash

      - name: Run tests
        run: |
          export PYO3_PYTHON=$(which python3)
          if [ "$RUNNER_OS" == "macOS" ]; then
            export DYLD_LIBRARY_PATH=$(python3 -c "import sysconfig; print(sysconfig.get_config_var('LIBDIR'))"):$DYLD_LIBRARY_PATH
          fi
          cargo test --verbose --lib
        shell: bash

      - name: Run doc tests
        run: |
          export PYO3_PYTHON=$(which python3)
          if [ "$RUNNER_OS" == "macOS" ]; then
            export DYLD_LIBRARY_PATH=$(python3 -c "import sysconfig; print(sysconfig.get_config_var('LIBDIR'))"):$DYLD_LIBRARY_PATH
          fi
          cargo test --doc
        shell: bash

  # Python tests
  python-test:
    name: Python Tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install maturin
        run: |
          if [ "$RUNNER_OS" == "Linux" ]; then
            pip install maturin[patchelf]
          else
            pip install maturin
          fi
        shell: bash

      - name: Build Python wheel
        run: |
          export PYO3_PYTHON=$(which python3)
          if [ "$RUNNER_OS" == "macOS" ]; then
            export DYLD_LIBRARY_PATH=$(python3 -c "import sysconfig; print(sysconfig.get_config_var('LIBDIR'))"):$DYLD_LIBRARY_PATH
          fi
          maturin build --release
        shell: bash

      - name: Install built wheel
        run: python -m pip install --find-links=target/wheels xlsxpress

      - name: Install test dependencies
        run: pip install pytest pytest-cov

      - name: Create test fixtures
        run: |
          mkdir -p tests/fixtures
          python -c "
          import xlsxpress
          w = xlsxpress.Writer()
          s = w.add_worksheet('Sheet1')
          w.write_string(s, 0, 0, 'Hello')
          w.write_number(s, 0, 1, 42.0)
          w.write_string(s, 1, 0, 'World')
          w.write_number(s, 1, 1, 3.14)
          w.write_string(s, 2, 0, 'Test')
          w.write_number(s, 2, 1, 100.0)
          w.save('tests/fixtures/test.xlsx')
          print('Test fixture created successfully')
          "
        shell: bash

      - name: Run Python tests
        run: pytest tests/python -v --cov=xlsxpress --cov-report=xml
        if: hashFiles('tests/python/**/*.py') != ''

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12' && hashFiles('tests/python/**/*.py') != ''
        with:
          file: ./coverage.xml
          flags: python
          name: python-${{ matrix.python-version }}

  # Security audit
  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Run security audit
        run: cargo audit

  # Check for minimum supported Rust version
  msrv:
    name: Check MSRV
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.70
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.70"

      - name: Check build with MSRV
        run: cargo check --all-features
